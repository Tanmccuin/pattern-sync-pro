/**
 * PSP store stub.
 *
 * Free-tier override storage now lives directly in the core/block block's
 * `pspOverrides` attribute in post_content — no separate REST store needed.
 *
 * This file is reserved for Pro-tier features: versioning, rollback,
 * audit log, and role-based permission state.
 */
